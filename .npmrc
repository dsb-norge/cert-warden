# Skip optional dependencies: the only ones in this tree are typescript's 20 per-platform
# native compiler binaries, pulled in as inert peer-dependency baggage (commitlint loads TS
# configs via jiti, and this repo's config is YAML). Nothing executes them; not installing
# them shrinks the third-party surface by ~20 packages in CI and in the commit-msg hook.
omit=optional
